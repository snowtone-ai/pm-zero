# pm-zero v13 作業台帳

更新日: 2026-09-07。作業ブランチ: `work/pm-zero-v13`。

## 現在地と次の操作

v13の作成、グローバル反映、旧版削除、関連検証は完了。残りは変更のコミットとGitHub反映・リモート確認。
開始時のユーザー原稿 `prompt.md` は未変更・未追跡のまま保持する。

## 確定した希望

- VS Code / PowerShellのCodex。製品種は固定せず、新規プロダクトごとに内容を具体化する。
- 質問は必要事項をまとめて一括提示。既知の回答と、AIが調べられる技術は質問し直さない。
- 既存のChatGPT Plus / Claude Pro等の月額AI契約以外の追加費用は0円。過剰実装・過剰テストを強く避ける。
- 利用枠が切れたら回復を待つ。AIが無断でモデル・サービスを切り替えたり追加購入したりしない。
- 既定の親Astra/xhighが判断、Luna/xhighが範囲の明確な実装。同時に子は最大2、孫なし。小作業は親だけで済ませる。
- ユーザーが明示的に親モデルを変える場合もある。コンパクションはモデル変更への追従を考慮する。
- 角丸0、カード/装飾パネル群なし等の見た目指定は維持する。
- 専用作業ブランチを使い、依頼範囲の環境構築から公開まで自律実行。既存の権限と独自連携を維持する。
- Claude Codeは将来の可能性。現時点で専用設定や実行基盤を作らない。

## Orderの状態

- [x] ① 公式Astra資料、Codex設定・委譲・利用枠を調査。Context7で実装を照合。
- [x] ① GitHub、Note、SNSの候補を評価。grill-me/ponytailの必要な原則だけ採用。
- [x] ② 利用環境、製品種、利用枠対応、予算、質問形式、見た目をヒアリングで確定。
- [x] ③ v13本体、共通指示・部分設定テンプレート、README、保守用AGENTSを作成。
- [x] ③ グローバルAGENTS/configへ反映。独自の認証・連携・権限設定は保持。
- [x] 追加依頼: 実験的コンパクションを有効化。50/60/70％を仮定付きで比較し、技術者のWeb一次資料を基にモデル既定を最終採用。
- [x] ④ 旧ナレッジと旧更新履歴を現行ツリーから削除。過去はGit履歴に保持。
- [x] ④ 必要な文書・設定検証を実行。
- [ ] ④ GitHubへ反映し、リモートのコミットと作業状態を確認。

## 実装と検証

- 本体: `pm-zero-knowledge-v13.md`。新規相談から環境構築・実装・公開・再開までを定義。
- 共通指示: `templates/AGENTS.global.md` の参照先だけ実パスに置換し、`~/.codex/AGENTS.md` へ配置。5,443→4,150 UTF-8 bytes。実トークン消費の削減率は未測定。
- config: 親の既存Astra/xhighを維持。子の既定をLuna/xhigh、同時上限4→2へ変更。
- コンパクション: `context_management = { experimental_mode = true }` を有効化。旧 `model_auto_compact_token_limit = 136000` は削除。モデル既定に追従し、手元では公称90％が基準。任意の％設定ではない。
- 調査・比較・採用理由・制約は `docs/decisions.md` D07〜D09。
- Codex CLI 0.153.4の公式schemaで対象キー・型を確認。Python 3.13.14 / tomllibで実configとテンプレートを解析し、管理キーの一致を確認。
- 変更前configとの構造比較: 指定した子の設定とコンパクション閾値以外に変更なし。認証・MCP・アプリ・権限等は保持。
- `codex features list`: `context_management under development true`、`remote_compaction_v2 stable true`。
- グローバルAGENTSとテンプレートの置換結果が一致。文書の内部ファイルリンク・アンカーと文字コードを確認。
- `git diff --check` とstage済み差分の空白検査は成功。UI/アプリはないため、ブラウザやアプリのテストは不要。
- 実モデルの長い試験会話・節約ベンチマークは実施していない。実験機能の実発火・履歴取得・節約率は未確認。新しい設定読込みからの適用を想定する。

## 退避と復旧

- 実験機能追加前: `~/.codex/backups/pm-zero-v13/config.before-experimental-context.20260907-022302.toml`。
- v13反映前: 同じフォルダの `AGENTS.md.before-v13.20260907-030853` と `config.toml.before-v13.20260907-030853`。
- 退避ファイルは公開しない。復旧時は対象キーだけを戻し、その後の無関係な変更を消さない。
- 実験機能に問題が出た場合は台帳を保存し、`features.context_management` の追加行を除いて通常経路へ戻す。

## Gitと再開

- 開始時: `main`、HEAD `5c261f0`。`work/pm-zero-v13` で実装。
- リモート: `snowtone-ai/pm-zero`（公開）、既定は `main`。反映前のfetchで既存mainとの分岐なし。
- 次の操作: 明示した変更だけコミット・pushし、mainへの反映を確認する。ユーザーの原稿と私用設定は含めない。
- 調査・検証の再実行は、新しい変更・版変更・失敗・未解決の問題がある場合だけ。

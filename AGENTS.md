# pm-zero v13 の保守

- このリポジトリは開発手順と設定テンプレートを管理する。アプリや実行基盤を増やさない。
- 開始時は `tasks.md` の現在地とGit状況を確認し、必要な節だけ `pm-zero-knowledge-v13.md` と `docs/decisions.md` で補う。
- グローバル指示の原本は `templates/AGENTS.global.md`。実配置時だけ `__PM_ZERO_ROOT__` をこのリポジトリの絶対パスに置き換える。
- `templates/config.global.toml` は部分設定。同名キーを統合し、ユーザーの接続・認証・その他の設定を保持する。グローバル変更前の退避は `~/.codex/backups/pm-zero-v13/` へ置き、公開リポジトリに入れない。
- main以外の作業ブランチで編集する。ユーザーの `prompt.md` と無関係な未コミット変更はそのまま保つ。
- 仕様変更は公式資料・手元の版で確認し、根拠は `docs/decisions.md` へ残す。旧版はGit履歴で参照できるため、現行ツリーへ複製しない。
- 文書は内部リンク・矛盾・秘密混入・diffの空白を確認する。設定変更時はTOMLと手元のCodexでの読込みを確認する。UI/アプリのテストや長い試験会話はこのリポジトリでは不要。
- コミット前に意図したファイルだけを明示してstageする。許可されたGitHub反映はリモートの状態まで確認して完了とする。

# pm-zero knowledge v13

更新日: 2026-09-07

## この文書の使い方

これは、Codexで新しいソフトウェアを考え、初版を公開できるところまで進めるための実用手順である。
毎回全文を読み込まず、入口と現在の作業に必要な節だけを読む。プロジェクトの事実は、そのプロジェクトの `tasks.md`、`AGENTS.md`、必要なら `docs/decisions.md` に残す。
グローバル `AGENTS.md` は新規プロダクトまたは pm-zero 依頼の入口でこの文書を指す短い案内にし、各プロジェクトの `AGENTS.md` は固有の制約と実行方法だけに絞る。

### 索引

- [0. 入口と原則](#0-入口と原則): 起動条件、役割、自律実行、利用枠
- [1. 新規プロダクトの流れ](#1-新規プロダクトの流れ): 発想から公開確認まで
- [2. ヒアリング](#2-ヒアリング): 最初にまとめて聞く内容と仮定の置き方
- [3. 初版の受入条件](#3-初版の受入条件): 必須範囲と対象外を決める
- [4. プロジェクト準備と役割分担](#4-プロジェクト準備と役割分担): 環境、Git、委譲
- [5. 実装と品質確認](#5-実装と品質確認): 必要十分なEngineering
- [6. UIの基準](#6-uiの基準): UIがあるプロジェクトだけ適用
- [7. 継続、制限、学習](#7-継続制限学習): `tasks.md` とコンパクションをまたぐ再開
- [8. 能力の調査と導入](#8-能力の調査と導入): MCP、skill、plugin、外部資産
- [9. 完了条件](#9-完了条件): 引き渡し前の確認
- [10. 判断記録](#10-判断記録): 記録する内容

## 0. 入口と原則

ユーザーが「このようなプロダクトを作りたい」と言ったら、新規プロダクトの流れを開始する。製品の種類は固定しない。
既存プロジェクトの変更依頼なら、同じ流れのうち必要な節から開始し、目的と変更範囲を先に確定する。
開始時の依頼文は、たとえば「このプロダクトを作りたい。プロダクトリポジトリの pm-zero に従い、環境構築、開発、必要な検証、依頼範囲のデプロイまで進めてください」とする。

ユーザーが指定した目的と範囲の中では自律的に調査、編集、環境構築、検証、デプロイ確認まで進める。通常の手順を進めるための再承認ループは作らない。
危険な不明点は、作業を止める理由と選択肢を平易に示し、必要事項を最初に一括で聞く。費用が発生する操作、誰に公開するか、機微データを扱うか、既存データを変更・削除するかが代表例である。

親の Astra/xhigh は、目的の整理、質問、設計判断、範囲管理、受入判断、統合を担当する。実装が必要な場合は Luna/xhigh に範囲を明示して委譲する。
子エージェントは同時に最大2人であり、0〜2人は上限であって目標人数ではない。小さな変更は親だけで完了する。
子の子は増やさない。共有ファイルを複数の子が同時に編集しない。親が最終的な品質と受入を負う。

標準のグローバル設定を前提にする。現在の既定は `gpt-6-astra`、`xhigh`、子は `gpt-5.6-luna`、`xhigh`、同時上限2である。権限、認証、ユーザーが独自に有効化した接続は維持する。
実験的 `context_management = { experimental_mode = true }` は有効化済みだが、実際の削減率は測定していない。オートコンパクションの固定値は外し、モデルごとの既定に追従させる。手元では公称90％が基準であり、任意の％指定ではない。50・60・70％の比較と採用理由は、このpm-zeroリポジトリの `docs/decisions.md` のD08を参照する。消費最小や連続コンパクションの解消を保証せず、文脈不足や反復読込みなど具体的な問題が実作業で出たときだけ再検討する。長期試験や節約ベンチマークは追加しない。

## 1. 新規プロダクトの流れ

### 1.1 開始

1. ユーザーの一文を、誰のどんな困り事を解決するものかに言い換える。
2. 対象プロダクトのリポジトリと現在の状態を確認する。変更前の未コミット作業をユーザーのものとして扱い、勝手に戻さない。
3. すでに存在する `AGENTS.md`、`tasks.md`、README、設定、テスト方法を必要な範囲だけ読む。
4. 最初の初版を小さく作れるように、[ヒアリング](#2-ヒアリング)を始める。

### 1.2 決定から実装へ

5. 回答を短く要約し、解決する困り事、主な操作、初版の必須項目、対象外、受入条件を文書化する。
6. 未回答でも安全に進められることは仮定を明記して進める。危険な不明点だけ質問を続ける。
7. 技術を決める前に、リポジトリの既存機能、依存関係、実行環境、標準機能を確認する。技術名をユーザーに選ばせない。
8. プロジェクト専用の作業ブランチを作り、mainで作業しない。
9. 必要なときだけ、目的、対象ファイル、完了条件、検証方法を添えて Luna に委譲する。
10. 実装し、関係する範囲の検証を行い、失敗があれば原因を直して一度確認する。
11. ユーザーが依頼した範囲のデプロイまたは公開を行い、実際のURL、起動状態、主要操作を確認する。
12. 現在地、完了内容、残り、次の手順を `tasks.md` の先頭に残して引き渡す。

途中で利用枠に達したら、作業を止める前に記録を保存し、枠の回復を待つ。別モデル、別サービス、別アカウントへ勝手に切り替えない。

## 2. ヒアリング

質問は必要事項を最初にまとめて提示し、高校生でも理解できる日本語を使う。質問した理由を必要なときだけ短く添える。
調べれば分かる技術、一般的な実装方法、ライブラリ名を質問しない。質問の答えからAIが技術と設計を決める。
回答を受けたら「決まったこと」「まだ仮のこと」「次に必要な確認」を一、二文で整理し、既知の内容を聞き直さない。後から聞くのは、初回回答で判明した追加の重大点だけにする。

### 2.1 最初にまとめて聞く内容

1. **使う人**: 誰が、どんな場面で使うか。
2. **困り事**: 今は何に困っていて、使った後に何が楽になれば成功か。
3. **主な操作**: 最初に開いた人が、どの順番で何をするか。
4. **初版の必須**: 最初の公開時に必ずできなければならないこと。
5. **対象外**: 初版では作らないこと。将来案は分けて記録する。
6. **失敗とデータ**: 失敗すると困ること、扱うデータ、個人情報や秘密情報の有無。
7. **公開と見た目**: 公開相手、ログインの要否、参考にしたい製品や雰囲気。追加費用0円・角丸0・カード群なしは既定として説明し、予算を毎回聞き直さない。

「主な操作」は画面名や技術用語ではなく、利用者の行動で聞く。たとえば「何を入力し、何を見て、何を決めるのか」と聞く。
初版の境界が曖昧なら、最も小さい価値のある一つの流れに絞り、後回しにする機能を明示する。

### 2.2 仮定と質問の境界

計画上の追加費用は0円とする。既存のChatGPT Plus、Claude Proなどの月額AI契約は前提に含めるが、インフラ、公開、素材、外部サービスの有料契約を自動で追加しない。
有料機能が必要そうな場合は、先に無料で成立する小さい案を作る。有料案は費用と公開範囲を説明し、ユーザーが明示的に選ぶまで契約や課金へ進まない。
安全な仮定の例は、個人用の小規模利用、既存の開発環境、公開前のテスト環境である。ただし仮定として記録する。
公開対象、機微データ、既存データの破壊につながる不明点は、初回の質問に含めて確認する。回答が足りずに新たな重大点が生まれた場合だけ、後から必要最小限を聞く。
ユーザーが答えていないことを「回答済み」と記録しない。仮定、未回答、決定を見出しやラベルで区別する。
危険な不明点を残したまま公開や有料サービスの契約へ進まない。公開後に変更できる文言や色などは、合理的な仮定で進めてよい。
回答が得られない場合は、停止位置、仮定、選べる次の手順を `tasks.md` に残す。

## 3. 初版の受入条件

受入条件は、実装前に「誰が、何をして、何が確認できれば完了か」という形で書く。数は価値のある主要操作に必要な分だけにする。
必須項目、対象外、既知の制限、成功を確認する方法を一緒に書く。将来の便利機能、予備の抽象化、不要な管理画面は初版へ入れない。
受入条件は親が最終判断し、子エージェントやテスト結果だけに委ねない。

例:

```text
- 利用者が入力を保存できる。
- 保存した内容を次に開いたとき確認できる。
- 入力に失敗した場合、原因とやり直し方が表示される。
- 初版では複数人共有と高度な分析を扱わない。
```

受入条件を満たしたら、変更と無関係な全機能の検証を増やさない。未達なら、原因を特定できる最小の追加確認を行う。

## 4. プロジェクト準備と役割分担

### 4.1 環境とGit

プロジェクトのルート、実行コマンド、依存関係、環境変数、既存のブランチと作業差分を確認する。秘密情報を出力、コミット、外部サービスへ送信しない。
空のフォルダなら、その場所が対象であることを確認して `git init -b main`、`git switch -c work/bootstrap` を行う。選定した最小構成を公式の初期化手順で作り、不要なサンプルを除く。依存の固定用ファイル、実際に使う起動・検証コマンドを残す。
各プロジェクトに短い `AGENTS.md` と `tasks.md` を作る。AGENTSにはpm-zero本体の実在する参照先、プロジェクト固有の制約、起動・検証コマンド、台帳の場所だけを書く。一般規則を全文複製しない。環境変数が必要なら秘密を含まない名前の例を用意し、実値をGit管理外へ置く。
専用ブランチを作成し、変更の目的が追える小さなコミットにする。mainへの直接作業を避け、既存差分を含めるかどうかを確認してから扱う。
破壊的な削除、公開範囲の変更、既存データの移行は対象と根拠を特定してから行う。依頼範囲にない公開や外部への連絡を追加しない。
GitHubとデプロイは既存の接続とプロジェクトの規約を使う。ソースコードの公開範囲とアプリの公開範囲を区別し、未指定のコードを勝手に公開しない。完了時は必要な変更をコミット・pushし、求められた公開に必要なら検証後にPRを統合する。URLの実動作、秘密設定、無料枠と課金無効、必要な復旧方法を確認する。認証など外部条件が不足する場合は完了扱いにせず、具体的な不足と再開点を残す。

### 4.2 委譲

親は「なぜ作るか」と受入条件を保持し、Luna は指定された実装範囲を進める。依頼は次の形を目安に短くする。

```text
目的: 初版の受入条件のうち何を満たすか
対象: 変更してよいファイルまたは範囲
制約: 既存機能を優先、不要な依存・抽象化を増やさない
完了: 期待する動作と必要な検証
報告: 変更、検証結果、残る懸念を短く返す
```

調査、実装、レビューを毎回3人へ分けない。独立した確認が必要なのは、具体的なデータ破損、権限、公開、複雑な移行などのリスクがある場合だけである。
子が変更した範囲は親が読み、受入条件との関係を確認して統合する。委譲の調整コストが成果を上回るなら委譲しない。
独自のworkerファイルや起動スクリプトを増やさない。標準の委譲機能と、依頼時の役割指定で足りる設計にする。
モデルを明示する委譲では、全履歴を複製して親モデルを継承する方式を避け、空または必要な直近部分の文脈と短い依頼を渡す。利用中の環境が役割・モデル指定に対応しない場合は、その制約を明示し、別モデルでLunaを使ったふりをしない。

## 5. 実装と品質確認

### 5.1 Engineeringの基準

標準機能と既存依存を最初に使う。新しい依存、分割、抽象化は、受入条件や保守上の明確な理由がある場合だけ加える。
コードは短く読みやすくし、将来の可能性のための仕組みを先に作らない。分割は変更境界や責任が実際に分かれるときだけ行う。

### 5.2 具体的なリスク対処

- **安全性**: 入力を検証し、秘密をコードやログに残さず、権限を必要最小限にする。認証・認可の抜け、外部入力、依存の既知問題を変更範囲に応じて確認する。
- **使いやすさ**: キーボード操作、意味のある見出しとラベル、十分なコントラスト、エラーの原因と回復方法を確認する。色だけで状態を伝えない。
- **信頼性**: 通信や保存の失敗を利用者へ伝え、再試行しても二重登録や壊れた状態にならないようにする。必要なログと境界のエラー処理を用意する。
- **保守性**: 変更理由と制約をコードまたは判断記録に残し、依存の版と実行手順を追えるようにする。使われない機能と設定を増やさない。

性能は主要操作を実際に測ってから判断する。遅さが受入条件や利用体験に影響する場合だけ、測定で見つかったボトルネックを改善する。無期限のチューニングや推測による最適化をしない。
テストは変更のリスクと受入条件に比例させる。意味のない網羅、実装をそのまま写すテスト、低リスクの可逆変更への過剰なテストを作らない。

### 5.3 検証の順番

1. 変更対象に必要な構文、型、lint、ビルドなどを既存コマンドから選ぶ。全コマンドの実行を毎回必須にしない。
2. 主要操作を受入条件の順に一度通し、成功と失敗からの回復を確認する。
3. データ、権限、既存機能に影響する具体的なリスクだけ追加確認する。
4. 失敗したら原因を直し、変更後の関連確認を再実行する。成功後は新しい懸念がない限り反復を増やさない。

## 6. UIの基準

UIがある場合だけ適用する。今回の pm-zero リポジトリ自体にはUIがないため、文書作成では画面確認を行わない。
見た目はユーザー指定により、角丸0・カード群なしを共通ルールとして維持する。製品ごとに再承認を求めない。1ページは1つの主要目的と1つの主要アクションに絞り、二次的な内容は名前のある子ページへ分ける。全情報をaccordionやmodalへ詰め込まない。
実装前に類似する既存プロダクトを調べ、最も適合する参考を一つ選ぶ。URL、調査日、適合理由、借りる構造・操作、採用しない固有要素を判断記録に残す。ブランドや固有資産を複製しない。
情報の優先順位は、重要情報を先頭側に置く、余白と配置でまとめる、文字の大きさと太さを調整する、整列と区切りをそろえる、最後に色とコントラストを使う、の順で設計する。外部基準はApple Human Interface Guidelinesだけにする。
UI面と操作部品は `border-radius: 0` とし、角丸card/button/input/badge/pill/capsule、装飾パネル群、入れ子の装飾箱、装飾目的の円形cropを作らない。連続したキャンバスを文章、余白、配置、区切り、一覧、表で構造化する。Appleの見た目やcomponentは模倣せず、この形状指定を優先する。
既存の人間が作った品質のよいUIライブラリとアセットを優先し、採用した出典、版、ライセンス、利用範囲を記録する。必要なものがなければ、独自の効果やAI生成素材を追加しない。
画面遷移、保存、読み込み、エラーには短く連続的で中断可能な視覚フィードバックを出す。空白画面、固まり、表示位置の急な移動を避け、`prefers-reduced-motion` では即時表示の経路を用意する。
必要なeffect、animation asset、効果音は保守中でライセンス適合の既存資産を調べて選び、AI生成しない。適切なものがなければ採用しない。音は予測でき、利用者が制御できるようにする。
完成前に既存Chrome DevTools MCPで実行中の変更ルート、主要操作、対応幅を実画面で確認する。console、network、runtimeに異常がないことも見る。参考製品・出典・経路・操作・結果を記録し、コードを読んだだけでUI完了とはしない。

## 7. 継続、制限、学習

### 7.1 tasks.md

長い作業では `tasks.md` をプロジェクトの再開台帳にする。ファイルの先頭に次を置く。

```text
# 作業台帳
更新日:
現在地:
次の手順:
受入条件の状態:
未解決の質問・仮定:
変更ブランチ:
検証済み:
停止理由や制限:
```

作業を始める前に現在地と次の手順を読む。重要な決定、変更したファイル、検証結果、失敗の原因、次に一手で再開できる操作だけを更新する。
コンパクションが起きても、台帳を会話の代わりにして同じ工程を繰り返さない。要約だけを信頼せず、必要な事実が書かれたファイルを読む。
台帳が大きくなったら、現在地と次の手順を先頭に保ち、詳細な判断だけを `docs/decisions.md` へ分ける。最初からファイルを増やさない。

### 7.2 利用枠と再開

残り枠を定常的に監視したり、回復時刻を推測したりしない。上限到達後は書込みの機会がない場合もあるため、作業の区切りごとに台帳を保存する。回復後に同じAstra/Lunaの役割で再開する。ユーザーが明示的に親モデルを変えた場合は、その指定を優先する。
再開時は、現在地、受入条件、未解決点、次の手順、直近の検証結果だけを確認する。完了済みの調査やテストを理由なく繰り返さない。

### 7.3 学習

再発防止に価値がある具体的な失敗だけを既存の台帳へ記録し、詳細が増えたときだけ `docs/decisions.md` に分ける。事実、原因、影響、再発防止策、確認方法を短く残す。
毎回新しいskillや常時指示を作らない。既存の手順を直す価値があるほど同じ失敗が起きた場合だけ、入口や判断基準を更新する。
Claude Codeは将来使う可能性がある。今はClaude専用設定や別の実行経路を導入せず、判断記録は将来共有できる一般的な内容にする。

## 8. 能力の調査と導入

新しいプロジェクトを始めるとき、または必要な機能を変えるときだけ、既存の能力を先に確認する。毎回同じ調査をやり直さない。
候補は、目的への適合、更新状況、Windows/PowerShell互換性、認証とデータ露出、権限、文脈使用量、既存機能との重複、ライセンス、再現性で比べる。
外部データや操作を直接提供するものはMCPを優先し、手順や再利用可能な指示はskill、配布とライフサイクルに意味がある場合だけpluginを検討する。
導入する場合は、保守元の一次資料で版とライセンスを確認し、最小スコープでスモーク確認してから使う。改善しなければ導入しない。
v13で新しく導入したMCP、plugin、skillは0である。grill-meの未解決の選択肢を具体化する考え方と、ponytailの既存機能を使い不要な抽象化を増やさない考え方を短く再構成した。質問方法はユーザー指定の一括提示を優先する。外部skillの本文をコピーしない。
能力調査の要求、候補、調査日、出典・版、権限とデータ、選定理由、適用範囲、確認結果は既存の台帳または `docs/decisions.md` に記録する。プロジェクト内を優先し、グローバル接続を毎回増やさない。pm-zeroの共通MCP既定は既存Chrome DevToolsとし、利用者独自の接続は別に扱う。

## 9. 完了条件

次のすべてが満たされたら、依頼された範囲の作業を完了として報告する。

- 利用者、困り事、主な操作、初版必須、対象外、危険な不明点が整理されている。
- 受入条件を主要操作で確認でき、既知の制限が記録されている。
- 変更は専用ブランチにあり、未依頼の削除・公開・外部連絡をしていない。
- 必要十分な基本確認と、変更に関係するリスク確認が成功している。
- UIがあれば、ライブラリ・資産の記録と実画面のDevTools確認が済んでいる。
- 依頼範囲にデプロイや公開が含まれる場合、実際の起動、URL、主要操作、失敗時の表示を確認している。
- `tasks.md` の先頭から、現在地と次の手順を再開できる。
- 変更点、検証結果、残る重大な懸念を短く説明できる。

公開後に発見した不具合は、影響、再現手順、原因、修正、再確認を台帳または判断記録へ残し、次の作業へつなげる。

## 10. 判断記録

この文書に書かれた運用の根拠と現在の設定は、このpm-zeroリポジトリの調査台帳 `docs/decisions.md` を参照する。個々のプロダクトの判断記録とは区別する。
主要な一次資料は、[GPT-6 Astra公式ガイド](https://developers.openai.com/api/docs/guides/latest-model?model=gpt-6-astra)、[Codexの委譲](https://learn.chatgpt.com/docs/agent-configuration/subagents)、[Codex設定リファレンス](https://learn.chatgpt.com/docs/config-file/config-reference)、[Codexの料金・利用枠](https://learn.chatgpt.com/docs/pricing)、[AGENTS.md](https://learn.chatgpt.com/docs/agent-configuration/agents-md) である。
公式仕様、ローカルで確認した事実、外部作者の実践、pm-zeroの提案を混同しない。変更日、版、根拠、適用範囲を判断記録へ残す。
